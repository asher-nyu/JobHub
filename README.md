# JobHub

JobHub is a Vue.js bookmark dashboard for job search links and related resources.

## Setup

Install dependencies:

```sh
npm install
```

## Development

Run the local development server:

```sh
npm run dev
```

## Production

Build the app:

```sh
npm run build
```

Preview the production build:

```sh
npm run preview
```
